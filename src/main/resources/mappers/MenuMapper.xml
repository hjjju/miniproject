<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hnworks.miniProject.domain.menu.mapper.MemberMapper">

    <!--    <select id="findById" parameterType="hashmap" resultType="hashmap">
            SELECT
            ID,
            NAME
            FROM
            ITEM
            WHERE
            ID = #{id}
        </select> -->

<!--    <select id="findById" parameterType="String" resultType="hashmap">
        SELECT
            ID,
            NAME
        FROM
            Member
        WHERE
            ID = #{id}
    </select>
    <insert id="save" parameterType="hashmap">
        INSERT INTO Member( ID, NAME)
        VALUES ( #{id}, #{name} )
    </insert>-->
</mapper>